services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    environment:
      - ALLOWED_HOSTS=0.0.0.0,127.0.0.1,localhost
      - DJANGO_DB_PATH=/app/data/db.sqlite3
    restart: unless-stopped

volumes:
  db_data:
